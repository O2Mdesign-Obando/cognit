# Demo2 source boundary

Demo2 is the independent React/Vite application imported from the read-only Figma Make export at:

`/Users/jorgeobando/Desktop/Figma Make demo/Cognit Learning Hub Demo - Figma`

The application is isolated in `demo2/` and builds at `/learninghub/demo2/`. Its React source, route structure, styles, and bundled visual assets are preserved. The only integration adaptations are the package name, Vite output/base paths, React Router basename, root workspace build orchestration, and the deployment rewrite required for direct SPA deep links.

Demo1 remains the Astro application at `/learninghub/demo/`.
